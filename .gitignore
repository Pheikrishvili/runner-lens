node_modules
coverage

# sourcemaps (large debug artifacts, not needed at runtime)
*.map
*.tsbuildinfo

# Generated preview files
gen-preview.*
preview-*

# Local-only living plan (matches the core/ and app/ repos)
PLAN.md
